const _ = require('underscore');
const {} = require('../lib/dotenv');
const request = require('superagent');

module.exports = {
	name: 'Symbol',

	mixins: [],
	actions: {
		get: {
			handler(ctx){

			},
		},
		update: {
			handler(ctx){

			},
		},
	},

	methods: {
	},
};
